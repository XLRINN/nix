# This is a placeholder encrypted file for SSH private key
# When you're on your NixOS system, run:
# agenix -e modules/nixos/secrets/ssh-key.age
# 
# Then paste your SSH private key (the content of your id_ed25519 file)
# This key will be used for outgoing SSH connections to other systems 