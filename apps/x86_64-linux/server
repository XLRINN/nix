#!/usr/bin/env bash
set -euo pipefail

# Mirror the x86 server installer to reuse the desktop workflow.
export NIX_REF=${NIX_REF:-server}
export NIX_BRANCH=${NIX_BRANCH:-server}
export NIX_PROFILE=${NIX_PROFILE:-server}

exec "$(dirname "$0")/desktop" "$@"
