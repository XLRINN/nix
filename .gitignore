# Ignore API keys and other secrets fetched from Bitwarden
modules/shared/config/api-keys/keys.env
modules/shared/config/tailscale/key

# These files are generated by secrets-wizard.sh (or sopswarden flows)
# and should not be committed to git